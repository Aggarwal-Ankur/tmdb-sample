Sample app to display latest movies from The Movie Database

Note : Android Studio Chipmunk may show compilation errors with 'androidTest' or 'test' package. However, all the tests run fine when they are run